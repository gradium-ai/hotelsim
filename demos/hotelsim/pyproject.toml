[project]
name = "hotelsim"
version = "0.1.0"
description = "Four Seasons Rive Gauche — bilingual hotel concierge voice agent"
requires-python = ">=3.12"
dependencies = [
    "gradbot",
]

[tool.uv.sources]
gradbot = { path = "../../gradbot_py", editable = true }
